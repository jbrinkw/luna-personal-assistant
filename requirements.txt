# Luna Core Requirements
# Python 3.10+

dotenv
# Web frameworks
fastapi==0.115.0
uvicorn==0.30.0
starlette==0.38.0
streamlit==1.39.0

# Database  
# Note: psycopg2-binary 2.9.9 doesn't work on Python 3.13
# Use psycopg (v3) for Python 3.13+ support, or use Python 3.12
psycopg[binary]>=3.1.0  # Modern psycopg3 with Python 3.13 support
psycopg-pool>=3.1.0  # Connection pooling for psycopg3
# psycopg2-binary>=2.9.9  # Legacy, only if using Python < 3.13

# LangChain and AI
langchain>=0.3.0
langchain-core>=0.2.27,<0.4
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
langgraph>=0.2.0

# MCP Server
fastmcp>=2.12.4
mcp[cli]

# OAuth 2.0 and Authentication
pyjwt==2.9.0
bcrypt==4.2.0

# Validation and utilities
pydantic>=2.12.0
python-dotenv>=1.1.0
aiohttp>=3.9.0

# Timezone support
pytz==2024.1

# Testing (optional)
pytest==8.3.0
pytest-asyncio==0.24.0

# Documentation
mkdocs>=1.5.0
mkdocs-material>=9.5.0

